# City of Guthrie Website

A static website for the City of Guthrie, Oklahoma.

## How to Run

Simply open `index.html` in a web browser, or use any static server:

```bash
# Using Python
python -m http.server 8000

# Using Node.js (npx)
npx serve .