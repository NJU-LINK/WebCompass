# Fortune Cookie

A digital fortune cookie web application. Click the cookie to reveal your fortune!

## How to Run

Simply open `index.html` in a web browser, or use any static server:

```bash
# Using Python
python -m http.server 8000

# Using Node.js (npx)
npx serve